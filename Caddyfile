# DOMAIN is injected via docker-compose from the root-level .env file.
# Replace with your real domain there, e.g. karengolf.example.com
{$DOMAIN} {
	handle /api/* {
		reverse_proxy backend:8000
	}

	handle /health {
		reverse_proxy backend:8000
	}

	handle {
		reverse_proxy frontend:80
	}
}
