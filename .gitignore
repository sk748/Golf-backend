venv
.env

# Python
__pycache__/
*.py[cod]

# Test / coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# CodeMender local state (agent workspace, logs, scan db)
.codemender/
.cm_project

# Node / Vite (frontend)
node_modules/
dist/
dist-ssr/
*.local
*.tsbuildinfo
vite.config.ts.timestamp-*
.eslintcache

# Editor / OS
.DS_Store

# Claude Code — per-checkout settings and worktrees (never committed)
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.claude/state.json

# Original upload archive (extracted to repo root)
Golf-backend-restructure-split-apps-phase0.zip

# Audit docs are tracked as files under docs/audit/ — the zip is a redundant artifact
docs/audit.zip
